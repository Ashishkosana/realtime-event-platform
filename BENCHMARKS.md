# Benchmarks

Run `python scripts/bench.py` after the lab is up. Do not invent numbers. Fill this table from `bench-results.json`.

Closest V2 trigger already taken: large recipient lists use chunked fan-out instead of one giant transaction.
