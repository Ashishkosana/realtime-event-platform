# Security

No auth on the HTTP adapter. HMAC `x-notify-signature` on real webhooks. Fake transports are lab-only. Bind localhost on shared machines. `DEMO_MODE` does not expose process kill in V1 of this repo.
